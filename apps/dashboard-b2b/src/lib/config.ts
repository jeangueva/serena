/**
 * La AGPL (sección 13) obliga a que un servicio en red ofrezca a sus usuarios
 * una forma de obtener el código que están usando. Si desplegás un fork, esta
 * URL tiene que apuntar al tuyo, no a este repositorio.
 */
export const SOURCE_URL = import.meta.env.VITE_SOURCE_URL ?? "https://github.com/jeangueva/serena";
